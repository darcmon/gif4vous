# Copy to .env — must match ADMIN_TOKEN in apps/api/.env
# (Local-only tool: fine here. In a public app, NEVER put secrets in
# frontend env vars — anything Vite exposes ships in the JS bundle.)
VITE_ADMIN_TOKEN=replace-with-the-same-token-as-the-api
